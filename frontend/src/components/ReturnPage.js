import React from 'react';
import './ReturnPage.css';

const ReturnPage = () => {
    return (
        <div className="return-page">
            <h1>Return a Cycle</h1>
            <p>To return a cycle, go to 'My Orders' page and click 'Return' next to the cycle you want to return.</p>
        </div>
    );
};

export default ReturnPage;
